<?php

namespace I18n;

/**
 *
 * @method translate($message, $count = NULL, $parameters = array(), $locale = NULL);
 */
interface ITranslator extends Nette\Localization\ITranslator
{
	// function translate($message, $count = NULL, array $parameters = array(), $locale = NULL);
}
